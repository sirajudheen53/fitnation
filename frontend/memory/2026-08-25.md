# 2026-08-25 — Sprint 5 UI Gaps (Pixel)

## Status: All 3 stories Complete ✅ — branch `sprint-5/ui-gaps` pushed

**Branch:** `sprint-5/ui-gaps` (from `sprint-5/razorpay`)
**Tests:** 48 new tests added; full suite 176/176 passing. 0 TS errors in new code.

### FBOS-023 — Branch Management UI (8 pts) ✅
- `types/branch.ts`, api fns in `lib/api.ts` (GET/POST /branches/, GET/PATCH /branches/{id}/)
- `features/branches/components/`: BranchForm (zod), BranchTable, BranchDetail
- Pages: list (search/filter/add/delete), new, `[id]` detail (stats grid), `[id]/edit`
- **Gap flagged:** backend has NO `DELETE /branches/{id}/` and NO stats endpoint. Delete UI targets DELETE (will 405 until Forge adds it); stats derived from `metadata.stats` (zeroed fallback).

### FBOS-024 — Trainer Performance UI (8 pts) ✅
- `types/trainer-performance.ts`, api: `GET /trainer-performance/`, `GET /trainers/{id}/performance/`, `GET /users/trainers/{id}/metrics/`
- `features/trainers/components/`: PerformanceTable (sort/filter), PerformanceCharts (recharts), PerformanceMetricCard
- Pages: `/trainers/performance` (list), `/trainers/[id]/performance` (detail: metric cards + revenue/rating line charts + sessions bar chart)
- Added "Trainer Performance" nav item (TrendingUp icon)

### FBOS-022 — Wati WhatsApp Settings (5 pts) ✅
- `types/notification.ts`, api wired to **Forge's real FBOS-021 endpoints**: GET/PATCH `/notifications/settings/`, POST `/notifications/test/`, GET `/notifications/logs/`
- `features/notifications/components/`: NotificationSettingsForm (toggle, masked API key, connection status, test button), NotificationLogsTable, LogDetailModal
- Pages: `/notifications/settings`, `/notifications/logs` (filters + detail modal)
- Added "Notifications" nav item (Bell icon)

## ⚠️ Blockers / Notes
1. **Pre-existing lint errors block `next build`** (NOT mine): `app/marketplace/cart/page.tsx`, `features/diet/components/DietPlanForm.tsx`, `features/workouts/components/WorkoutPlanForm.tsx` — `react-hooks/set-state-in-effect`. Production build fails on lint step. Needs fixing (Forge/PO) before deploy.
2. **Branch DELETE endpoint missing** (backend) — delete UI will 405.
3. Branch list endpoint returns plain array (not `{count, results}`) — handled with `Array.isArray` guard.
4. No dedicated "github" Hub agent visible in session list — pushed branch directly via git.

## Decisions
- Followed existing pattern: thin page wrappers in `app/`, presentational components + tested helpers in `features/`.
- Tests target presentational components/helpers (matches existing suite pattern), not full page data-fetching.

### FBOS-025 — Customer Detail Polish (13 pts) ✅
- Refactored `app/customers/[id]/page.tsx` into 6-tab interface: Overview | Fitness Goals | Body Measurements | Health Profile | Progress Photos | Payment History
- New components under `features/customers/components/`: CustomerTabs, OverviewTab, FitnessGoalsTab, BodyMeasurementsTab, HealthProfileTab, ProgressPhotosTab, PaymentHistoryTab
- Types in `types/customer-detail.ts` (HealthProfile, CustomerFitnessGoal, BodyMeasurement, ProgressPhoto, ProgressSummary)
- Wired to Forge's FBOS-025 endpoints (note: actual measurement path is `/measurements/`, NOT `/body-measurements/` as in the PO spec)
- Tests: 23 passing. Full suite now 199/199.
